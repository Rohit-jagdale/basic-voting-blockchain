compile and deploy voting.sol while deploying set candidates name in strings of array and set voting time in minutes 
replace contract ADDRESS and ABI with constants
```bash
npm install
npm start
```
